#ifndef VECTORS_HPP
#define VECTORS_HPP

#include<iostream>
#include<vector>
#include<algorithm>

namespace vectors{
    std::vector<int> reverse(std::vector<int>* old);
    int max(std::vector<int>* vec);
}

#endif
