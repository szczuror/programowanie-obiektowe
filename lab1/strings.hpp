#ifndef STRINGS_HPP
#define STRINGS_HPP

#include <iostream>
#include <string>

namespace strings{
    extern int COMBINE_COUNT;
    std::string combine_strings(std::string s1, std::string s2);
}
#endif
